---
epic: EP-003
story_id: us_003
title: Push Real-Time Task Status Updates via SignalR to Dashboard
persona: Care Team Member
priority: Must Have
points: 5
sprint: 2
status: Draft
---

# US-003: Push Real-Time Task Status Updates via SignalR to Dashboard

> **Epic:** EP-003 — AI Agent Orchestration Framework | **Sprint:** 2 | **Points:** 5 | **Priority:** Must Have

## User Story

**As a** care team member,
**I want** agent task status changes pushed to my dashboard in real time via SignalR without requiring a page refresh,
**so that** I always see the current state of the care transition workflow without delay.

## Acceptance Criteria

- [ ] **AC-1:** FastAPI backend implements a SignalR hub at `/hubs/dashboard` using `signalrcore` library; hub groups: `encounter-{encounter_id}`, `unit-{unit_name}`, `role-{role_name}`
- [ ] **AC-2:** When any `AgentTask.status` changes (PENDING→IN_PROGRESS, IN_PROGRESS→COMPLETE, etc.): Coordinator Agent calls `POST /api/v1/internal/signalr/notify` → hub broadcasts `TaskStatusChanged` event to group `encounter-{encounter_id}` within 1 second
- [ ] **AC-3:** Angular `SignalRService` connects to hub with JWT Bearer auth; subscribes to `TaskStatusChanged` and `ADTEventReceived` events; updates dashboard state without page reload
- [ ] **AC-4:** `ADTEventReceived` event broadcast to `unit-{unit_name}` group whenever new ADT event processed — drives live ADT feed on dashboard
- [ ] **AC-5:** SignalR hub Cloud Run service configured with min-instances=2 to ensure <1 second latency
- [ ] **AC-6:** Integration test: update AgentTask status via API → measure time until Angular `TaskStatusChanged` handler fires → must be ≤1 second

## Definition of Done

- [ ] SignalR hub deployed and accepting WebSocket connections
- [ ] Angular client tested in integration with real SignalR hub (not mock)
- [ ] Latency <1 second confirmed in 10 consecutive integration test runs

## Dependencies

- EP-003 US-001 (AgentTask records exist)
- EP-TECH US-001 (Cloud Run SignalR hub provisioned)
